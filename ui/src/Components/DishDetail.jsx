import React from 'react';
import PropTypes from 'prop-types';

const { dish } = this.props;

const DishDetail = () => (
  <div>
  

  </div>
);

DishDetail.proptype = {
  dish: PropTypes.array.required,

};
export default DishDetail;
